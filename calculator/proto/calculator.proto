syntax = "proto3";

package calculator;

import "sum.proto";
import "primes.proto";

option go_package = "github.com/umermasood/grpc-golang-codealong/calculator/proto";

service CalculatorService {
  rpc Sum(SumRequest) returns (SumResponse);
  rpc Primes(PrimeRequest) returns (stream PrimeResponse);
}
